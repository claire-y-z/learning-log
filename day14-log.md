## Day 14-7/13/26

What I did: Completed Kaggle Pandas lesson on Summary Functions and Maps (using describe(), mean(), unique(), map()) to transform data in examples, worked through stats review.

Notes: Noticed an interesting difference: SQL uses DISTINCT, R (dplyr) uses distinct() and Pandas uses .unique(), but logic remains the same under the different syntax.